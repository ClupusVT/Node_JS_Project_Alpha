<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        /* Add CSS styles to make links look like buttons */
        .button-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            margin-right: 10px;
        }

        /* Additional styling for the Logout button */
        .logout-button {
            background-color: #e74c3c;
        }
    </style>
</head>
<body>
    <h2>User Profile</h2>
    <p>Welcome, <%= username %>!</p>
    <p>Email: <%= userEmail %></p>

    <!-- Include a form for updating user data -->
    <form action="/user/update_profile" method="post">
        <!-- Add hidden input field for username -->
        <input type="hidden" name="username" value="<%= username %>">

        <!-- Add input fields for updating data -->
        <input type="text" name="newFullName" placeholder="New Full Name">
        <input type="email" name="newEmail" placeholder="New Email"> 
        <button type="submit">Update Profile</button>
    </form>

    <br>
    <!-- Style the links as buttons -->
    <a href="/dashboard" class="button-link">Back to Dashboard</a>
    <a href="/logout" class="button-link logout-button">Logout</a>
</body>
</html>
